#include <iostream>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, k;
        cin >> n >> k;
        int a[n];
        int b[n];
        for (int i=0; i<n; i++) {
            cin >> a[i];
            // cin >> b[i];
        }
        for (int i=0; i<n; i++) {
            // cin >> a[i];
            cin >> b[i];
        }
        int sumA = 0 , sumB = 0;
        // while (1) {
        //     if (k = 0) {
                
        //     }
        // }

    }
    return 0;
}